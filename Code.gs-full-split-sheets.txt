// Google Apps Script final version for split sheets.
// Replace the whole current Code.gs with this code.

var MIDDLE_RESULT_EMAIL = "coconanwon@gmail.com"; // 중등부 결과를 받을 이메일 주소
var WORD_CHAIN_SOURCE_SHEETS = ["Voca_토플", "Voca_토익"];
var WORD_CHAIN_WORDS_SHEET = "WordChainWords";
var WORD_CHAIN_STATE_SHEET = "WordChainState";
var WORD_CHAIN_ENTRIES_SHEET = "WordChainEntries";
var WORD_CHAIN_HINT_COUNT = 5;
var WORD_CHAIN_DICTIONARY_VERSION = "2";
var WORD_CHAIN_LEADERBOARD_LIMIT = 5;
var CROSSWORD_RESULTS_SHEET = "CrosswordResults";
var CROSSWORD_LEADERBOARD_LIMIT = 10;
var WRITING_CORRECTION_PROMPT = "AI OPERATING INSTRUCTIONS (DO NOT EDIT)\nYou are a TOEFL-style AI rater & writing coach for a Korean academy.\nYour job: grade and coach a student's TOEFL Writing response using the official ETS task rubric internally, then report a student-friendly estimated Writing level on the 1-6 CEFR-aligned scale. The displayed band is an academy estimate from this response, not an official TOEFL Writing section score.\n⚙️ CORE POLICIES (ALWAYS APPLY)\n1. Output Language\nAll feedback written in Korean, except for English examples, revised sentences, and rubric terms.\n2. Vocabulary Policy (CRITICAL)\nDo NOT encourage difficult or advanced vocabulary.\nPrioritize clear, natural, commonly used words a Korean high school student can realistically reproduce under timed pressure.\nWhen suggesting vocabulary upgrades, choose words that improve clarity and precision, NOT difficulty.\nKeep revised language and functional sentence frames easy to reproduce under time pressure, but never supply a generic memorized paragraph that could ignore the actual prompt.\nNever suggest GRE-level words (e.g., obfuscate, ameliorate, paradigmatic).\n3. Typo & Minor Error Policy\nDo not use a fixed typo-count threshold. A few common slips may occur even in a strong timed response.\nFlag spelling and mechanics when they obscure meaning, recur enough to weaken control, or reveal a pattern the student should fix.\nDo not impose an automatic deduction for an isolated common typo; judge its effect together with the overall clarity and error pattern.\n4. Coaching Priority Order\nGive feedback that realistically moves the student's estimated level up one half-band (for example, 4 to 4.5). Treat this as an academy coaching target, not an official single-task score conversion. In every response, prioritize fixes in this order:\nTask fulfillment (did they address all bullets / answer the question?)\nStructure & organization (Email: clear communicative flow / Discussion: position or contribution → relevant support → clear ending)\nElaboration & development (reasons + specific examples)\nCoherence (logical flow, linking devices)\nRegister / task conventions (Email: audience and politeness; Discussion: relevant contribution and engagement with the posted views)\nSentence-level grammar & vocabulary\nSpelling & punctuation (lowest priority)\n5. Full Revised Version Policy\nThe Full Revised Version is the student's personalized model answer.\nKeep student's original ideas, stance, and examples — do NOT add new claims or change position.\nKeep vocabulary at the student's current realistic level + 1 small upgrade notch.\nUse a natural high-performing structure suited to the actual prompt, but do not force a fixed template or cite one in the feedback.\n📥 SECTION 0: INPUT DATA\nTarget Level: [Beginner / Intermediate / Advanced]\nTask Type: [Write an Email / Academic Discussion]\nTopic/Prompt: (paste the original prompt here)\nStudent Response: (paste student writing here)\n📏 SECTION 1: OFFICIAL ETS TASK RUBRICS + STUDENT-FACING ESTIMATED LEVEL\nA. Student Display Table — 1–6 + CEFR (ACADEMY ESTIMATE, NOT AN OFFICIAL SINGLE-TASK SCORE)\nB. Official ETS 0–5 Task Rubric Anchors (INTERNAL ONLY — DO NOT PRINT)\nWrite an Email — What each score looks like\n5 (internal ETS task score): Fully successful, clear, and effective; strong elaboration supports the communicative purpose; syntactic variety, precise word choice, appropriate social conventions, and almost no errors beyond ordinary timed-writing slips.\n4 (internal ETS task score): Generally successful and easily understood; adequate elaboration, useful syntactic variety, appropriate word choice, mostly appropriate social conventions, and few errors.\n3 (internal ETS task score): Partially successful; generally accomplishes the task, but limitations reduce clarity or effectiveness; development is partial and noticeable language or convention errors remain.\n2 (internal ETS task score): Mostly unsuccessful; the response attempts the task but has limited or partly irrelevant development, a restricted range of connected language, or an accumulation of errors.\n1 (internal ETS task score): Unsuccessful; very little development, telegraphic or disconnected language, serious frequent errors, or minimal original language.\n0 (internal ETS task score): Blank, rejects the topic, is not in English, is entirely copied, is entirely unconnected to the prompt, or consists of arbitrary keystrokes.\nAcademic Discussion — What each score looks like\n5 (internal ETS task score): A very clear, relevant contribution with well-elaborated explanations, examples, or details; effective syntactic variety and precise word choice; almost no errors beyond ordinary timed-writing slips.\n4 (internal ETS task score): A relevant contribution whose ideas are easily understood; adequate explanations, examples, or details; a useful range of structures and appropriate word choice; few errors.\n3 (internal ETS task score): A mostly relevant and understandable contribution, but part of the explanation, example, or detail may be missing, unclear, or irrelevant; noticeable language errors remain.\n2 (internal ETS task score): An attempt to contribute, but ideas are poorly developed or only partly relevant; structural and vocabulary range is limited and errors may make ideas difficult to follow.\n1 (internal ETS task score): An ineffective attempt with few or no coherent ideas, severely limited language, serious frequent errors, or minimal original language.\n0 (internal ETS task score): Blank, rejects the topic, is not in English, is entirely copied, is entirely unconnected to the prompt, or consists of arbitrary keystrokes.\nC. Critical Scoring Rules\n100-word guidance for Discussion: ETS says an effective response will contain at least 100 words. Do not impose an automatic score cap solely because a response is shorter; judge whether the actual response is sufficiently relevant, developed, clear, and controlled.\nEmail has no official hard minimum. Use the academy word-count range as a practice target and judge whether the communicative purpose is adequately developed.\nStudent-facing half bands: Report an estimated 1–6 band in half-point increments with CEFR. Internally consult the official 0–5 task rubric, but do not print that raw task score for students and do not claim a mechanical conversion.\nReport one student-facing result: 'Estimated Writing Level: X/6 (CEFR)'. Do not report an official Writing section score or a /30 score from one Email or Discussion response.\n📐 SECTION 2: WORD COUNT GOALS (MATCH GENERATION PROMPT)\nWrite an Email — Academy Practice Targets (No Official Hard Minimum)\nAcademic Discussion — Academy Practice Targets (ETS Recommends 100+ Words)\nA response under 100 words is not automatically capped. Note the shortfall and evaluate its actual effect on development, relevance, clarity, and language control.\n✅ SECTION 3: DIAGNOSTIC CHECKLISTS (BY TASK TYPE)\nA. Write an Email — Diagnostic Checklist\n[Task Fulfillment]\n[ ] Addresses Bullet 1 clearly\n[ ] Addresses Bullet 2 clearly\n[ ] Addresses Bullet 3 clearly\n[ ] Achieves the communicative purpose (request/complaint/feedback/etc.)\n[Structure & Organization]\n[ ] Greeting fits the recipient when one is used; do not deduct automatically if the message begins effectively without a separate greeting\n[ ] Purpose is clear early in the email; the exact phrase 'I am writing to' is optional\n[ ] All requested functions are addressed in a coherent order; the original bullet order is not mandatory\n[ ] Closing line is appropriate when useful; do not require a stock closing sentence\n[ ] Sign-off or signature is appropriate when used; do not treat its absence alone as a major scoring fault\n[Social Conventions — Rubric Core]\n[ ] Register matches recipient (formal for professor/editor/manager; semi-formal for coworker)\n[ ] Polite request formulation present if applicable (\"Could you please...\" / \"Would it be possible...\")\n[ ] Polite complaint framing if applicable (\"I have noticed...\" / \"Unfortunately...\")\n[ ] Register stays consistent throughout (no mixing formal/casual)\n[Elaboration]\n[ ] The important requested functions are adequately developed with relevant reasons or details; do not require one because-clause or example for every bullet\n[ ] No vague filler like \"It is good\" / \"I like it\" without support\n[Language Use]\n[ ] Subject-verb agreement reasonable\n[ ] Tenses consistent\n[ ] Articles (a/the) reasonable\n[ ] Prepositions reasonable\n[ ] Higher-level target: Uses natural syntactic variety appropriate to the message; no concessive, passive, or participle quota\nB. Academic Discussion — Diagnostic Checklist\n[Task Fulfillment]\n[ ] Clear position or contribution stated; it may agree, disagree, extend a view, or take an independent position\n[ ] Directly answers the professor's question\n[ ] Aims for the ETS-recommended 100+ words; no automatic score cap for a shorter response\n[Contribution — Rubric Core]\n[ ] Optional: May reference a classmate by name, but receives no automatic reward or penalty for doing so\n[ ] Makes a contribution in the student's own words rather than copying or merely repeating a posted claim\n[ ] Contributes by extending, countering, qualifying, or adding an independent reason/example; a completely new angle is not mandatory\n[ ] Stays clearly relevant to the professor's question; repeating a particular key term is optional\n[Structure & Coherence]\n[ ] Opening makes the student's position or contribution clear; classmate reference is optional\n[ ] Middle: reason + concrete example/detail\n[ ] Development adds the student's own reasoning, detail, example, qualification, or counterpoint\n[ ] A concise wrap-up may help, but is not required if the contribution already ends clearly\n[ ] Ideas connect logically; no fixed transition-word list or quota\n[Elaboration]\n[ ] Main reason given\n[ ] Concrete example/detail provided (\"For instance...\" / \"A clear example is...\")\n[ ] Elaboration is sufficient for the claim; no fixed developmental-sentence count\n[Language Use]\n[ ] Subject-verb agreement / tense / articles reasonable\n[ ] Variety of sentence structures (not all simple sentences)\n[ ] Higher-level target: Uses flexible, natural structures and precise language; no mandatory concessive, passive, participle, or new-angle sentence\n🎯 SECTION 4: LEVEL-SPECIFIC COACHING FOCUS\nBeginner (realistic target: 3 → 3.5 → 4)\nFocus: task completion + basic organization + grammar control\nDo NOT push for advanced syntax or sophisticated vocabulary\nPraise short, clear sentences that achieve the task\nMain goal: student completes all bullets + stays on topic + uses basic linking devices (and, but, because, so)\nIntermediate (realistic target: 4 → 4.5 → 5)\nFocus: elaboration + sentence variety + register matching\nEncourage relevant reasons and concrete details where useful; do not force one example for every bullet\nIntroduce mild syntactic variety: compound sentences, relative clauses, some subordinating conjunctions (although, while, since)\nRegister: match formality to recipient precisely\nAdvanced (realistic target: 5 → 5.5 → 6)\nFocus: C1 syntactic variety + idiomatic precision + genuine contribution\nEncourage flexible C1-level control when natural; never force named grammar structures or count them mechanically\nWord choice: precise and idiomatic (but still accessible — no GRE words)\nDiscussion: the student's own contribution may extend, counter, qualify, or add an independent point; naming classmates is optional\nEmail: diplomatic nuance in requests/refusals\n📝 SECTION 5: OUTPUT FORMAT (COPY EXACTLY)\n## 📊 TOEFL Writing Score Report\n| Target Level | [Beginner / Intermediate / Advanced] |\n| :--- | :--- |\n| Task Type | [Write an Email / Academic Discussion] |\n| Word Count | ___ words / Goal: ___ |\n| Estimated Writing Level | ___/6 ([A1 / A2 / B1 / B2 / C1 / C2]) |\n| Score Meaning | Academy estimate from this response; not an official section score |\n✅ Strengths (잘한 점) — 2개\n... (구체적으로 어느 문장의 어느 부분이 잘 됐는지)\n...\n🛑 Highest-Impact Fixes (가장 큰 감점 원인) — 1~3개\n(Priority: Task > Structure > Elaboration > Coherence > Register > Grammar > Spelling)\n[제목: 문제 유형] 현재: [학생 글에서 문제 부분 인용] 문제: [왜 점수가 깎이는지] 개선: [구체적으로 어떻게 고쳐야 하는지]\n[필요한 경우에만 추가: 실제 점수에 영향을 주는 문제가 없으면 개수를 채우지 말 것]\n[필요한 경우에만 추가: 사소한 오타를 큰 감점 원인으로 부풀리지 말 것]\n✍️ High-Impact Line Editing (문장력 업그레이드) — 1~5개\n(오타보다 문법/표현/논리/톤 개선 위주. \"왜\"를 반드시 짧게 설명.)\nOriginal: [학생 문장 그대로] ➤ Better: [수정안] 💡 Why: [왜 이게 더 좋은지 한 줄 — 문법·표현·register·clarity 중 하나]\n(실제로 도움이 되는 문장만 1~5개. 개수를 맞추려고 이미 좋은 문장을 불필요하게 바꾸지 말 것.)\n🎯 Upgrade Toolkit (점수 올리는 장치)\n▸ Useful Connections (논리를 실제로 개선하는 경우만 — 0~3개):\n... (단순히 어려운 연결어로 바꾸지 말고, 학생 글의 실제 논리 관계에 맞는 표현만 제안)\n▸ High-Value Sentence Patterns (1~2개, 유연한 암기용):\nPattern 1: [영문 패턴 + 한국어 설명] 예: \"While [Student A] raises a valid point about ___, I believe ___.\" (선택형: 학생 이름 언급은 필수가 아님)\nPattern 2: [영문 패턴 + 한국어 설명]\n▸ Topic Vocabulary Upgrades (실제로 유용한 쉬운 3~5개):\nword : 한국어 뜻\nword : 한국어 뜻\nword : 한국어 뜻\n(주의: GRE급 어휘와 주제에 맞지 않는 암기 문단 금지. 시험장에서 실제로 쓸 수 있는 표현만.)\n✨ Full Revised Version (리라이팅)\n(아래 두 가지 규칙:)\n(1) 실제 과제에 맞는 자연스러운 구조를 따를 것 (Email: 목적과 요청 기능을 명확히 전개 / Discussion: 입장·자기 기여 → 이유·예시 → 필요할 때 선택적 학생 의견 연결). 학생 이름, 새 관점 문장, 고정 결론은 필수가 아님.\n(2) 학생의 원래 주장·입장·예시는 그대로 유지. 표현·문법·구조만 업그레이드. 새로운 주장 추가 금지.\n[여기에 완성본을 써넣기]\n🚀 Next Step (쌤의 한마디)\n[학생에게 한 문장 코칭. 형식: \"다음엔 ___만 고치면 좋은 점수 받을 수 있음\" 스타일]\n🔒 INTERNAL QA CHECK (Silently verify before submitting feedback — do NOT print)\n[ ] Word count accurately calculated and compared to Section 2 goal\n[ ] Estimated Writing Level is calibrated from the official task rubric and CEFR descriptors, then displayed as X/6 (CEFR)\n[ ] CEFR label matches the displayed academy estimate\n[ ] No official Writing section score or /30 score is claimed from this single response\n[ ] If Discussion is under 100 words, no automatic cap was applied; the actual effect on development and clarity was evaluated\n[ ] The 1–3 fixes follow Coaching Priority Order, and no issue was invented merely to fill a quota\n[ ] Line Editing items are genuinely useful, not padded with typo-only or unnecessary rewrites\n[ ] Vocabulary suggestions are all level-appropriate and simple (no GRE words)\n[ ] Full Revised Version preserves student's ideas and stance\n[ ] For Discussion: contribution is relevant and in the student's own words; classmate naming, a wholly new angle, and repeated professor wording were not required\n[ ] For Email: verified register matching, polite formulation, complete bullets\n[ ] For higher levels: natural language range was evaluated without forcing a named C1 structure quota\n[ ] Feedback targets ONE half-band upgrade, not perfection";

function doPost(e) {
  // 단어 테스트 비밀번호 확인
  if (e.parameter.action === "checkVocaPassword") {
    return checkVocaPassword_(e);
  }

  if (e.parameter.action === "playWordChain") {
    return playWordChain_(e);
  }

  if (e.parameter.action === "saveCrosswordResult") {
    return saveCrosswordResult_(e);
  }

  // 내신/모의고사 테스트 완료 기록 저장
  if (e.parameter.action === "saveTest") {
    return saveTestCompletion_(e);
  }

  // TOEFL 작문 제출 저장
  if (e.parameter.action === "saveWriting") {
    return saveWritingSubmission_(e);
  }

  // 학생이 이름, 문제, 암호로 최근 작문 결과 찾기
  if (e.parameter.action === "findWritingSubmission") {
    return findWritingSubmission_(e);
  }

  // 단어장 성적표 저장 기능
  if (e.parameter.action === "saveScore") {
    var scoreSheet = getScoresSheet_();
    if (scoreSheet) {
      scoreSheet.appendRow([
        new Date(),
        e.parameter.studentName,
        e.parameter.subjectDay,
        e.parameter.range,
        e.parameter.score
      ]);
    }
    sendMiddleVocaResultEmail_(e);
    return ContentService.createTextOutput("성공");
  }

  // 관리자 화면에서 방 만들기 저장 기능
  var passageSheet = SpreadsheetApp.getActiveSpreadsheet().getSheetByName("Passages");
  if (passageSheet) {
    passageSheet.appendRow([
      e.parameter.grade,
      e.parameter.subject,
      e.parameter.set,
      e.parameter.password,
      "",
      new Date()
    ]);
    CacheService.getScriptCache().remove("writingSetPasswordMap_v1");
  }
  return ContentService.createTextOutput("성공");
}

function checkVocaPassword_(e) {
  var subject = String(e.parameter.subject || "").trim();
  var day = String(e.parameter.day || "").trim();
  var password = String(e.parameter.password || "");
  var gradeKey = getVocaGradeKey_(day);
  var ss = SpreadsheetApp.getActiveSpreadsheet();
  var sheet = ss.getSheetByName("VocaPasswords");
  var exactPassword = null;
  var defaultPassword = null;

  if (sheet) {
    var rows = sheet.getDataRange().getValues();
    for (var i = 1; i < rows.length; i++) {
      var rowSubject = String(rows[i][0] || "").trim();
      var rowDay = String(rows[i][1] || "").trim();
      var rowPassword = String(rows[i][2] || "");

      if (rowSubject !== subject) continue;

      if (rowDay === day || (gradeKey && rowDay === gradeKey)) {
        exactPassword = rowPassword;
        break;
      }

      if (rowDay === "" && defaultPassword === null) {
        defaultPassword = rowPassword;
      }
    }
  }

  var correctPassword = exactPassword !== null ? exactPassword : defaultPassword;
  var ok = correctPassword !== null && password === correctPassword;

  return ContentService
    .createTextOutput(JSON.stringify({ ok: ok }))
    .setMimeType(ContentService.MimeType.JSON);
}

function getVocaGradeKey_(day) {
  var value = String(day || "").trim();
  var match = value.match(/^(중[1-3]|고[1-3])/);
  return match ? match[1] : "";
}

function saveCrosswordResult_(e) {
  var puzzleId = String(e.parameter.puzzleId || "").replace(/[^0-9A-Za-z_-]/g, "").slice(0, 40);
  var studentName = normalizeWordChainPlayerName_(e.parameter.studentName);
  var elapsedSeconds = Math.ceil(Number(e.parameter.elapsedSeconds || 0));

  if (!puzzleId || !studentName || !isFinite(elapsedSeconds) || elapsedSeconds < 1 || elapsedSeconds > 86400) {
    return crosswordJson_({ ok: false });
  }

  var sheet = getCrosswordResultsSheet_();
  var rows = sheet.getLastRow() > 1 ? sheet.getRange(2, 1, sheet.getLastRow() - 1, 4).getValues() : [];
  var bestRow = 0;
  var bestSeconds = Infinity;

  for (var i = 0; i < rows.length; i++) {
    if (String(rows[i][1] || "") !== puzzleId || String(rows[i][2] || "") !== studentName) continue;
    var savedSeconds = Number(rows[i][3] || 0);
    if (savedSeconds > 0 && savedSeconds < bestSeconds) {
      bestSeconds = savedSeconds;
      bestRow = i + 2;
    }
  }

  if (!bestRow) {
    sheet.appendRow([new Date(), puzzleId, studentName, elapsedSeconds]);
  } else if (elapsedSeconds < bestSeconds) {
    sheet.getRange(bestRow, 1, 1, 4).setValues([[new Date(), puzzleId, studentName, elapsedSeconds]]);
  }

  return crosswordJson_({
    ok: true,
    leaderboard: getCrosswordLeaderboardData_(sheet, puzzleId)
  });
}

function getCrosswordLeaderboard_(e) {
  var puzzleId = String(e.parameter.puzzleId || "").replace(/[^0-9A-Za-z_-]/g, "").slice(0, 40);
  if (!puzzleId) return crosswordJson_({ ok: false, leaderboard: [] });
  return crosswordJson_({
    ok: true,
    leaderboard: getCrosswordLeaderboardData_(getCrosswordResultsSheet_(), puzzleId)
  });
}

function getCrosswordResultsSheet_() {
  var ss = SpreadsheetApp.getActiveSpreadsheet();
  var sheet = ss.getSheetByName(CROSSWORD_RESULTS_SHEET);
  if (!sheet) {
    sheet = ss.insertSheet(CROSSWORD_RESULTS_SHEET);
    sheet.appendRow(["timestamp", "puzzleId", "studentName", "elapsedSeconds"]);
  }
  return sheet;
}

function getCrosswordLeaderboardData_(sheet, puzzleId) {
  if (sheet.getLastRow() < 2) return [];

  var rows = sheet.getRange(2, 1, sheet.getLastRow() - 1, 4).getValues();
  var bestByStudent = {};
  rows.forEach(function(row) {
    if (String(row[1] || "") !== puzzleId) return;
    var name = normalizeWordChainPlayerName_(row[2]);
    var elapsedSeconds = Number(row[3] || 0);
    if (!name || elapsedSeconds < 1) return;

    if (!bestByStudent[name] || elapsedSeconds < bestByStudent[name].elapsedSeconds) {
      bestByStudent[name] = {
        studentName: name,
        elapsedSeconds: elapsedSeconds,
        timestamp: row[0] instanceof Date ? row[0].getTime() : 0
      };
    }
  });

  var ranking = Object.keys(bestByStudent).map(function(name) {
    return bestByStudent[name];
  });
  ranking.sort(function(a, b) {
    if (a.elapsedSeconds !== b.elapsedSeconds) return a.elapsedSeconds - b.elapsedSeconds;
    return a.timestamp - b.timestamp;
  });

  return ranking.slice(0, CROSSWORD_LEADERBOARD_LIMIT).map(function(entry, index) {
    return {
      rank: index + 1,
      studentName: maskStudentName_(entry.studentName),
      elapsedSeconds: entry.elapsedSeconds
    };
  });
}

function crosswordJson_(data) {
  return ContentService
    .createTextOutput(JSON.stringify(data))
    .setMimeType(ContentService.MimeType.JSON);
}

function getWordChain_(e) {
  var lock = LockService.getScriptLock();
  var hasLock = false;

  try {
    lock.waitLock(25000);
    hasLock = true;

    var ss = SpreadsheetApp.getActiveSpreadsheet();
    ensureWordChainDictionary_(ss, false);
    var state = getOrCreateWordChainState_(ss);
    return wordChainJson_(buildWordChainPayload_(ss, state, normalizeWordChainPlayerName_(e.parameter.studentName)));
  } catch (error) {
    return wordChainJson_({
      ok: false,
      message: "게임 단어를 불러오지 못했습니다. 잠시 후 다시 시도해 주세요."
    });
  } finally {
    if (hasLock) lock.releaseLock();
  }
}

function playWordChain_(e) {
  var lock = LockService.getScriptLock();
  var hasLock = false;

  try {
    lock.waitLock(25000);
    hasLock = true;

    var ss = SpreadsheetApp.getActiveSpreadsheet();
    ensureWordChainDictionary_(ss, false);
    var state = getOrCreateWordChainState_(ss);
    var playerName = normalizeWordChainPlayerName_(e.parameter.studentName);
    var requestedWord = normalizeWordChainWord_(e.parameter.word);

    if (!playerName) {
      return wordChainFailure_(ss, state, playerName, "이름을 다시 입력해 주세요.");
    }

    if (!requestedWord) {
      return wordChainFailure_(ss, state, playerName, "영어 알파벳으로 된 4글자 이상 단어만 입력해 주세요.");
    }

    if (requestedWord.charAt(0) !== state.requiredLetter) {
      return wordChainFailure_(
        ss,
        state,
        playerName,
        "다른 학생이 방금 단어를 이어 썼습니다. 지금은 " + state.requiredLetter.toUpperCase() + "로 시작하는 단어 차례입니다."
      );
    }

    var entry = getWordChainEntry_(ss, requestedWord);
    if (!entry) {
      return wordChainFailure_(ss, state, playerName, "TOEFL·TOEIC 단어장에 있는 단어로 입력해 주세요.");
    }

    var usedWords = parseWordChainUsedWords_(state.usedWords);
    if (usedWords[entry.word]) {
      return wordChainFailure_(ss, state, playerName, "오늘 이미 나온 단어입니다. 다른 단어를 골라 주세요.");
    }

    var nextUsedWords = wordChainUsedWordsWith_(state.usedWords, entry.word);
    var nextUsedMap = parseWordChainUsedWords_(nextUsedWords);
    var nextCandidates = getWordChainAvailableEntries_(ss, entry.lastLetter, nextUsedMap);
    if (!nextCandidates.length) {
      return wordChainFailure_(
        ss,
        state,
        playerName,
        "그 단어 뒤에는 이어갈 단어가 없습니다. 다른 단어를 골라 주세요."
      );
    }

    var nextTotal = Number(state.totalWords || 0) + 1;
    var nextPlayerCounts = incrementWordChainPlayerCount_(state.playerCounts, playerName);
    var now = new Date();
    state.sheet.getRange(state.rowNumber, 1).setNumberFormat("@");
    state.sheet.getRange(state.rowNumber, 1, 1, 10).setValues([[
      state.dateKey,
      state.seedWord,
      entry.word,
      entry.meaning,
      entry.lastLetter,
      nextTotal,
      now,
      playerName,
      nextUsedWords,
      nextPlayerCounts
    ]]);

    var entriesSheet = getWordChainEntriesSheet_(ss);
    entriesSheet.getRange(entriesSheet.getLastRow() + 1, 1).setNumberFormat("@");
    entriesSheet.appendRow([
      state.dateKey,
      nextTotal,
      now,
      playerName,
      entry.word,
      entry.meaning,
      String(e.parameter.hintUsed || "") === "TRUE",
      entry.source
    ]);

    state.currentWord = entry.word;
    state.currentMeaning = entry.meaning;
    state.requiredLetter = entry.lastLetter;
    state.totalWords = nextTotal;
    state.updatedAt = now;
    state.lastPlayer = playerName;
    state.usedWords = nextUsedWords;
    state.playerCounts = nextPlayerCounts;

    clearWordChainWeeklyLeaderboardCache_();

    var payload = buildWordChainPayload_(ss, state, playerName);
    payload.acceptedWord = entry.word;
    payload.acceptedMeaning = entry.meaning;
    payload.relatedWords = getWordChainRelatedWords_(ss, entry.firstLetter, entry.word);
    return wordChainJson_(payload);
  } catch (error) {
    return wordChainJson_({
      ok: false,
      message: "등록하지 못했습니다. 잠시 후 다시 시도해 주세요."
    });
  } finally {
    if (hasLock) lock.releaseLock();
  }
}

function wordChainFailure_(ss, state, playerName, message) {
  var payload = buildWordChainPayload_(ss, state, playerName);
  payload.ok = false;
  payload.message = message;
  return wordChainJson_(payload);
}

function wordChainJson_(data) {
  return ContentService
    .createTextOutput(JSON.stringify(data))
    .setMimeType(ContentService.MimeType.JSON);
}

function refreshWordChainWords() {
  var ss = SpreadsheetApp.getActiveSpreadsheet();
  var sheet = ensureWordChainDictionary_(ss, true);
  return "WordChainWords updated: " + Math.max(sheet.getLastRow() - 1, 0) + " words";
}

function ensureWordChainDictionary_(ss, forceRefresh) {
  var properties = PropertiesService.getScriptProperties();
  var sourceSignature = getWordChainSourceSignature_(ss);
  var sheet = ss.getSheetByName(WORD_CHAIN_WORDS_SHEET);

  if (!forceRefresh && sheet && sheet.getLastRow() > 1 && properties.getProperty("wordChainSourceSignature") === sourceSignature) {
    return sheet;
  }

  var uniqueWords = {};
  WORD_CHAIN_SOURCE_SHEETS.forEach(function(sourceName) {
    var sourceSheet = ss.getSheetByName(sourceName);
    if (!sourceSheet || sourceSheet.getLastRow() < 2) return;

    var rows = sourceSheet.getDataRange().getValues();
    for (var i = 1; i < rows.length; i++) {
      var word = normalizeWordChainWord_(rows[i][4]);
      var meaning = String(rows[i][3] || "").trim();
      if (!word || word.length < 4 || !meaning || uniqueWords.hasOwnProperty(word)) continue;

      uniqueWords[word] = {
        source: sourceName,
        day: String(rows[i][1] || ""),
        word: word,
        meaning: meaning,
        firstLetter: word.charAt(0),
        lastLetter: word.charAt(word.length - 1)
      };
    }
  });

  var entries = [];
  Object.keys(uniqueWords).forEach(function(word) {
    entries.push(uniqueWords[word]);
  });
  entries = keepWordChainSafeEntries_(entries);

  if (!entries.length) {
    throw new Error("No usable TOEFL or TOEIC word-chain entries");
  }

  if (!sheet) sheet = ss.insertSheet(WORD_CHAIN_WORDS_SHEET);
  sheet.clearContents();
  sheet.getRange(1, 1, 1, 6).setValues([[
    "source", "day", "word", "meaning", "firstLetter", "lastLetter"
  ]]);
  sheet.getRange(2, 1, entries.length, 6).setValues(entries.map(function(entry) {
    return [entry.source, entry.day, entry.word, entry.meaning, entry.firstLetter, entry.lastLetter];
  }));

  clearWordChainLetterCache_();
  properties.setProperty("wordChainSourceSignature", sourceSignature);
  return sheet;
}

function getWordChainSourceSignature_(ss) {
  return WORD_CHAIN_SOURCE_SHEETS.map(function(sourceName) {
    var sheet = ss.getSheetByName(sourceName);
    return sourceName + ":" + (sheet ? sheet.getLastRow() : 0);
  }).join("|") + "|rules:" + WORD_CHAIN_DICTIONARY_VERSION;
}

function keepWordChainSafeEntries_(entries) {
  var current = entries.slice();
  var changed = true;

  while (changed) {
    var startCounts = {};
    current.forEach(function(entry) {
      startCounts[entry.firstLetter] = Number(startCounts[entry.firstLetter] || 0) + 1;
    });

    var next = current.filter(function(entry) {
      return Number(startCounts[entry.lastLetter] || 0) > 0;
    });
    changed = next.length !== current.length;
    current = next;
  }

  return current;
}

function clearWordChainLetterCache_() {
  var cache = CacheService.getScriptCache();
  for (var code = 97; code <= 122; code++) {
    cache.remove("wordChainLetter_" + String.fromCharCode(code));
  }
}

function getWordChainWordsSheet_(ss) {
  return ss.getSheetByName(WORD_CHAIN_WORDS_SHEET);
}

function getWordChainEntriesForLetter_(ss, firstLetter) {
  var letter = String(firstLetter || "").toLowerCase();
  if (!/^[a-z]$/.test(letter)) return [];

  var cache = CacheService.getScriptCache();
  var cacheKey = "wordChainLetter_" + letter;
  var cached = cache.get(cacheKey);
  if (cached) {
    try {
      return JSON.parse(cached);
    } catch (error) {
      cache.remove(cacheKey);
    }
  }

  var sheet = getWordChainWordsSheet_(ss);
  if (!sheet || sheet.getLastRow() < 2) return [];

  var rows = sheet.getRange(2, 1, sheet.getLastRow() - 1, 6).getValues();
  var entries = rows.filter(function(row) {
    return String(row[4] || "").toLowerCase() === letter;
  }).map(function(row) {
    return {
      source: String(row[0] || ""),
      day: String(row[1] || ""),
      word: String(row[2] || ""),
      meaning: String(row[3] || ""),
      firstLetter: String(row[4] || ""),
      lastLetter: String(row[5] || "")
    };
  });

  var serialized = JSON.stringify(entries);
  if (serialized.length < 90000) cache.put(cacheKey, serialized, 1800);
  return entries;
}

function getWordChainEntry_(ss, word) {
  var entries = getWordChainEntriesForLetter_(ss, word.charAt(0));
  for (var i = 0; i < entries.length; i++) {
    if (entries[i].word === word) return entries[i];
  }
  return null;
}

function getWordChainAvailableEntries_(ss, firstLetter, usedWords) {
  return getWordChainEntriesForLetter_(ss, firstLetter).filter(function(entry) {
    return !usedWords[entry.word];
  });
}

function getWordChainStateSheet_(ss) {
  var sheet = ss.getSheetByName(WORD_CHAIN_STATE_SHEET);
  if (!sheet) {
    sheet = ss.insertSheet(WORD_CHAIN_STATE_SHEET);
    sheet.appendRow([
      "dateKey", "seedWord", "currentWord", "currentMeaning", "requiredLetter",
      "totalWords", "updatedAt", "lastPlayer", "usedWords", "playerCounts"
    ]);
  }
  return sheet;
}

function getWordChainEntriesSheet_(ss) {
  var sheet = ss.getSheetByName(WORD_CHAIN_ENTRIES_SHEET);
  if (!sheet) {
    sheet = ss.insertSheet(WORD_CHAIN_ENTRIES_SHEET);
    sheet.appendRow([
      "dateKey", "sequence", "timestamp", "playerName", "word", "meaning", "hintUsed", "source"
    ]);
  }
  return sheet;
}

function getOrCreateWordChainState_(ss) {
  var sheet = getWordChainStateSheet_(ss);
  var dateKey = getWordChainDateKey_();
  var state = findWordChainState_(sheet, dateKey);
  if (state) return state;

  var seed = getWordChainSeed_(ss);
  var rowNumber = sheet.getLastRow() + 1;
  var now = new Date();
  var values = [[
    dateKey,
    seed.word,
    seed.word,
    seed.meaning,
    seed.lastLetter,
    0,
    now,
    "",
    wordChainUsedWordsWith_("", seed.word),
    "[]"
  ]];
  sheet.getRange(rowNumber, 1).setNumberFormat("@");
  sheet.getRange(rowNumber, 1, 1, 10).setValues(values);
  return wordChainStateFromRow_(sheet, rowNumber, values[0]);
}

function findWordChainState_(sheet, dateKey) {
  if (sheet.getLastRow() < 2) return null;
  var rows = sheet.getRange(2, 1, sheet.getLastRow() - 1, 10).getValues();
  var latestIndex = -1;
  var latestUpdatedAt = -1;

  for (var i = 0; i < rows.length; i++) {
    if (wordChainDateKeyFromValue_(rows[i][0]) !== dateKey) continue;
    var updatedAt = rows[i][6] instanceof Date ? rows[i][6].getTime() : new Date(rows[i][6]).getTime();
    if (isNaN(updatedAt)) updatedAt = 0;
    if (latestIndex === -1 || updatedAt >= latestUpdatedAt) {
      latestIndex = i;
      latestUpdatedAt = updatedAt;
    }
  }

  if (latestIndex === -1) return null;
  return wordChainStateFromRow_(sheet, latestIndex + 2, rows[latestIndex]);
}

function wordChainStateFromRow_(sheet, rowNumber, row) {
  return {
    sheet: sheet,
    rowNumber: rowNumber,
    dateKey: wordChainDateKeyFromValue_(row[0]),
    seedWord: String(row[1] || ""),
    currentWord: String(row[2] || ""),
    currentMeaning: String(row[3] || ""),
    requiredLetter: String(row[4] || "").toLowerCase(),
    totalWords: Number(row[5] || 0),
    updatedAt: row[6],
    lastPlayer: String(row[7] || ""),
    usedWords: String(row[8] || ""),
    playerCounts: String(row[9] || "[]")
  };
}

function getWordChainSeed_(ss) {
  var sheet = getWordChainWordsSheet_(ss);
  if (!sheet || sheet.getLastRow() < 2) throw new Error("Word-chain dictionary is empty");

  var rows = sheet.getRange(2, 1, sheet.getLastRow() - 1, 6).getValues();
  var entries = rows.map(function(row) {
    return {
      source: String(row[0] || ""),
      day: String(row[1] || ""),
      word: String(row[2] || ""),
      meaning: String(row[3] || ""),
      firstLetter: String(row[4] || ""),
      lastLetter: String(row[5] || "")
    };
  });
  var startCounts = {};
  entries.forEach(function(entry) {
    startCounts[entry.firstLetter] = Number(startCounts[entry.firstLetter] || 0) + 1;
  });
  var seedEntries = entries.filter(function(entry) {
    var nextCount = Number(startCounts[entry.lastLetter] || 0);
    if (entry.firstLetter === entry.lastLetter) nextCount--;
    return nextCount >= WORD_CHAIN_HINT_COUNT;
  });
  if (!seedEntries.length) throw new Error("No safe word-chain seed");

  var seed = seedEntries[Math.floor(Math.random() * seedEntries.length)];
  return {
    source: seed.source,
    day: seed.day,
    word: seed.word,
    meaning: seed.meaning,
    firstLetter: seed.firstLetter,
    lastLetter: seed.lastLetter
  };
}

function buildWordChainPayload_(ss, state, playerName) {
  var usedWords = parseWordChainUsedWords_(state.usedWords);
  return {
    ok: true,
    dateKey: state.dateKey,
    currentWord: state.currentWord,
    currentMeaning: state.currentMeaning,
    requiredLetter: state.requiredLetter,
    totalCount: Number(state.totalWords || 0),
    myCount: getWordChainPlayerCount_(state.playerCounts, playerName),
    lastPlayerName: state.lastPlayer ? maskStudentName_(state.lastPlayer) : "",
    hints: getWordChainHints_(ss, state.requiredLetter, usedWords),
    weeklyLeaderboard: getWordChainWeeklyLeaderboard_(ss, WORD_CHAIN_LEADERBOARD_LIMIT),
    recentEntries: getWordChainRecentEntries_(ss, state.dateKey, 10)
  };
}

function getWordChainHints_(ss, requiredLetter, usedWords) {
  return pickWordChainEntries_(
    getWordChainAvailableEntries_(ss, requiredLetter, usedWords),
    WORD_CHAIN_HINT_COUNT
  ).map(function(entry) {
    return entry.meaning;
  });
}

function getWordChainRelatedWords_(ss, firstLetter, acceptedWord) {
  var entries = getWordChainEntriesForLetter_(ss, firstLetter).filter(function(entry) {
    return entry.word !== acceptedWord;
  });
  return pickWordChainEntries_(entries, 3).map(function(entry) {
    return { word: entry.word, meaning: entry.meaning };
  });
}

function pickWordChainEntries_(entries, limit) {
  var pool = entries.slice();
  for (var i = pool.length - 1; i > 0; i--) {
    var randomIndex = Math.floor(Math.random() * (i + 1));
    var temp = pool[i];
    pool[i] = pool[randomIndex];
    pool[randomIndex] = temp;
  }
  return pool.slice(0, limit);
}

function getWordChainRecentEntries_(ss, dateKey, limit) {
  var sheet = getWordChainEntriesSheet_(ss);
  if (sheet.getLastRow() < 2) return [];

  var startRow = Math.max(2, sheet.getLastRow() - 499);
  var rows = sheet.getRange(startRow, 1, sheet.getLastRow() - startRow + 1, 8).getValues();
  var entries = [];
  for (var i = rows.length - 1; i >= 0 && entries.length < limit; i--) {
    if (wordChainDateKeyFromValue_(rows[i][0]) !== dateKey) continue;
    entries.push({
      playerName: maskStudentName_(rows[i][3]),
      word: String(rows[i][4] || ""),
      meaning: String(rows[i][5] || "")
    });
  }
  return entries;
}

function getWordChainWeeklyLeaderboard_(ss, limit) {
  var weekStartKey = getWordChainWeekStartKey_();
  var cache = CacheService.getScriptCache();
  var cacheKey = "wordChainWeeklyLeaderboard_" + weekStartKey + "_" + limit;
  var cached = cache.get(cacheKey);
  if (cached) {
    try {
      return JSON.parse(cached);
    } catch (error) {
      cache.remove(cacheKey);
    }
  }

  var sheet = getWordChainEntriesSheet_(ss);
  if (sheet.getLastRow() < 2) return [];

  var startRow = Math.max(2, sheet.getLastRow() - 4999);
  var rows = sheet.getRange(startRow, 1, sheet.getLastRow() - startRow + 1, 8).getValues();
  var counts = {};
  var todayKey = getWordChainDateKey_();

  for (var i = rows.length - 1; i >= 0; i--) {
    var entryDateKey = wordChainDateKeyFromValue_(rows[i][0]);
    if (entryDateKey < weekStartKey) break;
    if (entryDateKey > todayKey) continue;

    var name = normalizeWordChainPlayerName_(rows[i][3]);
    if (!name) continue;
    counts[name] = Number(counts[name] || 0) + 1;
  }

  var ranking = Object.keys(counts).map(function(name) {
    return { name: name, count: counts[name] };
  });
  ranking.sort(function(a, b) {
    if (b.count !== a.count) return b.count - a.count;
    return a.name < b.name ? -1 : (a.name > b.name ? 1 : 0);
  });

  var result = ranking.slice(0, limit).map(function(entry, index) {
    return {
      rank: index + 1,
      playerName: maskWordChainLeaderboardName_(entry.name),
      count: entry.count
    };
  });

  cache.put(cacheKey, JSON.stringify(result), 120);
  return result;
}

function clearWordChainWeeklyLeaderboardCache_() {
  var cache = CacheService.getScriptCache();
  var weekStartKey = getWordChainWeekStartKey_();
  cache.remove("wordChainWeeklyLeaderboard_" + weekStartKey + "_" + WORD_CHAIN_LEADERBOARD_LIMIT);
}

function getWordChainWeekStartKey_() {
  var parts = getWordChainDateKey_().split("-");
  var date = new Date(Date.UTC(Number(parts[0]), Number(parts[1]) - 1, Number(parts[2])));
  var daysSinceMonday = (date.getUTCDay() + 6) % 7;
  date.setUTCDate(date.getUTCDate() - daysSinceMonday);
  return date.toISOString().slice(0, 10);
}

function maskWordChainLeaderboardName_(value) {
  var chars = Array.from(normalizeWordChainPlayerName_(value).replace(/\s+/g, ""));
  if (!chars.length) return "";
  var visibleLength = chars.length >= 3 ? 2 : 1;
  return chars.slice(0, visibleLength).join("") + "*";
}
function getWordChainDateKey_() {
  return Utilities.formatDate(new Date(), Session.getScriptTimeZone() || "Asia/Tokyo", "yyyy-MM-dd");
}

function wordChainDateKeyFromValue_(value) {
  if (value instanceof Date) {
    return Utilities.formatDate(value, Session.getScriptTimeZone() || "Asia/Tokyo", "yyyy-MM-dd");
  }
  return String(value || "").trim();
}

function normalizeWordChainWord_(value) {
  var word = String(value || "").trim();
  if (!/^[A-Za-z]{4,}$/.test(word)) return "";
  return word.toLowerCase();
}

function normalizeWordChainPlayerName_(value) {
  return String(value || "")
    .replace(/[\r\n\t]+/g, " ")
    .replace(/\s+/g, " ")
    .replace(/[<>]/g, "")
    .trim()
    .slice(0, 20);
}

function parseWordChainUsedWords_(value) {
  var result = {};
  String(value || "").split("|").forEach(function(word) {
    if (word) result[word] = true;
  });
  return result;
}

function wordChainUsedWordsWith_(value, word) {
  var used = String(value || "");
  var token = "|" + word + "|";
  if (used.indexOf(token) !== -1) return used;
  return used + token;
}

function getWordChainPlayerCount_(serializedCounts, playerName) {
  if (!playerName) return 0;
  var counts = parseWordChainPlayerCounts_(serializedCounts);
  for (var i = 0; i < counts.length; i++) {
    if (counts[i].name === playerName) return Number(counts[i].count || 0);
  }
  return 0;
}

function incrementWordChainPlayerCount_(serializedCounts, playerName) {
  var counts = parseWordChainPlayerCounts_(serializedCounts);
  for (var i = 0; i < counts.length; i++) {
    if (counts[i].name === playerName) {
      counts[i].count = Number(counts[i].count || 0) + 1;
      return JSON.stringify(counts);
    }
  }
  counts.push({ name: playerName, count: 1 });
  return JSON.stringify(counts);
}

function parseWordChainPlayerCounts_(value) {
  try {
    var counts = JSON.parse(String(value || "[]"));
    return Array.isArray(counts) ? counts : [];
  } catch (error) {
    return [];
  }
}

function getScoresSheet_() {
  var ss = SpreadsheetApp.getActiveSpreadsheet();
  var sheet = ss.getSheetByName("Scores") || ss.getSheetByName("scores");

  if (!sheet) {
    sheet = ss.insertSheet("Scores");
    sheet.appendRow([
      "timestamp",
      "studentName",
      "subjectDay",
      "range",
      "score"
    ]);
  }

  return sheet;
}

function saveTestCompletion_(e) {
  var ss = SpreadsheetApp.getActiveSpreadsheet();
  var sheet = ss.getSheetByName("test");

  var headers = [
    "timestamp",
    "studentName",
    "grade",
    "subject",
    "problemSet",
    "questionCount",
    "correctCount",
    "wrongCount",
    "score",
    "completed"
  ];

  if (!sheet) {
    sheet = ss.insertSheet("test");
    sheet.appendRow(headers);
  } else if (sheet.getLastRow() === 0) {
    sheet.appendRow(headers);
  }

  sheet.appendRow([
    new Date(),
    e.parameter.studentName || "",
    e.parameter.grade || "",
    e.parameter.subject || "",
    e.parameter.problemSet || "",
    e.parameter.questionCount || "",
    e.parameter.correctCount || "",
    e.parameter.wrongCount || "",
    e.parameter.score || "",
    e.parameter.completed || "TRUE"
  ]);

  sendMiddleTestResultEmail_(e);

  return ContentService.createTextOutput("성공");
}

function getWritingSubmissionsSheet_() {
  var ss = SpreadsheetApp.getActiveSpreadsheet();
  var sheet = ss.getSheetByName("WritingSubmissions");
  var headers = [
    "timestamp",
    "studentName",
    "grade",
    "subject",
    "problemSet",
    "writingType",
    "targetLevel",
    "timeLimitSeconds",
    "elapsedSeconds",
    "wordCount",
    "prompt",
    "content",
    "copyPrompt",
    "submissionId",
    "feedback"
  ];

  if (!sheet) {
    sheet = ss.insertSheet("WritingSubmissions");
    sheet.appendRow(headers);
  } else if (sheet.getLastRow() === 0) {
    sheet.appendRow(headers);
  } else {
    ensureWritingSubmissionHeaders_(sheet, headers);
  }

  return sheet;
}

function ensureWritingSubmissionHeaders_(sheet, headers) {
  var currentHeaders = sheet.getRange(1, 1, 1, headers.length).getValues()[0];
  headers.forEach(function(header, index) {
    if (!currentHeaders[index]) {
      sheet.getRange(1, index + 1).setValue(header);
    }
  });
}

function saveWritingSubmission_(e) {
  var sheet = getWritingSubmissionsSheet_();
  var prompt = htmlToPlainText_(e.parameter.prompt || "");
  var content = String(e.parameter.content || "").trim();
  var writingType = inferWritingType_(e.parameter.writingType, prompt);
  var data = {
    targetLevel: String(e.parameter.targetLevel || "Intermediate"),
    writingType: writingType,
    prompt: prompt,
    content: content
  };
  var copyPrompt = buildWritingCopyPrompt_(data);
  var submissionId = Utilities.getUuid();

  sheet.appendRow([
    new Date(),
    e.parameter.studentName || "",
    e.parameter.grade || "",
    e.parameter.subject || "",
    e.parameter.problemSet || "",
    data.writingType,
    data.targetLevel,
    e.parameter.timeLimitSeconds || "",
    e.parameter.elapsedSeconds || "",
    e.parameter.wordCount || "",
    prompt,
    content,
    copyPrompt,
    submissionId,
    ""
  ]);

  return ContentService
    .createTextOutput(JSON.stringify({
      result: "success",
      submissionId: submissionId
    }))
    .setMimeType(ContentService.MimeType.JSON);
}

function buildWritingCopyPrompt_(data) {
  return getWritingCorrectionPrompt_() + [
    "",
    "",
    "--- STUDENT SUBMISSION (use this as Section 0 input) ---",
    "Target Level: " + data.targetLevel,
    "Task Type: " + data.writingType,
    "Topic/Prompt:",
    data.prompt,
    "",
    "Student Response:",
    data.content
  ].join("\n");
}

function inferWritingType_(submittedType, prompt) {
  var promptSource = String(prompt || "").toLowerCase();
  var submittedSource = String(submittedType || "").toLowerCase();

  // The original question is the authority when a set name or browser value is unclear.
  if (/(?:write\s+(?:an?\s+)?e-?mail|e-?mail|이메일)/i.test(promptSource)) {
    return "Write an Email";
  }
  if (/(?:academic\s+discussion|discussion\s+post|토론)/i.test(promptSource)) {
    return "Academic Discussion";
  }

  if (/(?:write\s+(?:an?\s+)?e-?mail|e-?mail|이메일)/i.test(submittedSource)) {
    return "Write an Email";
  }
  if (/(?:academic\s+discussion|discussion\s+post|토론)/i.test(submittedSource)) {
    return "Academic Discussion";
  }

  return "Academic Discussion";
}

function getWritingCorrectionPrompt_() {
  return String(WRITING_CORRECTION_PROMPT || "")
    .replace(/^Exit code:[\s\S]*?Output:\n/, "");
}

function htmlToPlainText_(value) {
  return String(value || "")
    .replace(/<br\s*\/?>/gi, "\n")
    .replace(/<\/p>/gi, "\n")
    .replace(/<[^>]*>/g, "")
    .replace(/&amp;/g, "&")
    .replace(/&lt;/g, "<")
    .replace(/&gt;/g, ">")
    .replace(/&quot;/g, '"')
    .replace(/&#0*39;/g, "'");
}

function getWritingSubmission_(e) {
  var submissionId = String(e.parameter.submissionId || "").trim();
  var result = { found: false };

  if (!submissionId) {
    return ContentService
      .createTextOutput(JSON.stringify(result))
      .setMimeType(ContentService.MimeType.JSON);
  }

  var sheet = getWritingSubmissionsSheet_();
  var lastRow = sheet.getLastRow();
  if (lastRow < 2) {
    return ContentService
      .createTextOutput(JSON.stringify(result))
      .setMimeType(ContentService.MimeType.JSON);
  }

  var match = sheet
    .getRange(2, 14, lastRow - 1, 1)
    .createTextFinder(submissionId)
    .matchEntireCell(true)
    .findNext();

  if (!match) {
    return ContentService
      .createTextOutput(JSON.stringify(result))
      .setMimeType(ContentService.MimeType.JSON);
  }

  var row = sheet.getRange(match.getRow(), 1, 1, 15).getValues()[0];
  result = {
    found: true,
    submittedAt: formatWritingSubmittedAt_(row[0]),
    problemSet: String(row[4] || ""),
    writingType: String(row[5] || ""),
    targetLevel: String(row[6] || ""),
    elapsedSeconds: Number(row[8] || 0),
    wordCount: Number(row[9] || 0),
    content: String(row[11] || ""),
    feedback: String(row[14] || "")
  };

  return ContentService
    .createTextOutput(JSON.stringify(result))
    .setMimeType(ContentService.MimeType.JSON);
}

function formatWritingSubmittedAt_(value) {
  if (!(value instanceof Date)) return String(value || "");
  var timeZone = Session.getScriptTimeZone() || "Asia/Tokyo";
  return Utilities.formatDate(value, timeZone, "yyyy.MM.dd HH:mm");
}

function getWritingResultSets_() {
  return writingResultJson_({
    sets: Object.keys(getWritingSetPasswordMap_()).sort()
  });
}

function getWritingSetPasswordMap_() {
  var cache = CacheService.getScriptCache();
  var cacheKey = "writingSetPasswordMap_v1";
  var cached = cache.get(cacheKey);

  if (cached) {
    try {
      return JSON.parse(cached);
    } catch (error) {
      cache.remove(cacheKey);
    }
  }

  var ss = SpreadsheetApp.getActiveSpreadsheet();
  var sheet = ss.getSheetByName("Passages");
  var setMap = {};

  if (sheet && sheet.getLastRow() > 1) {
    var rows = sheet.getRange(2, 1, sheet.getLastRow() - 1, 4).getValues();
    rows.forEach(function(row) {
      var grade = normalizeGradeName_(row[0]);
      var subject = String(row[1] || "").trim();
      var problemSet = String(row[2] || "").trim();
      if (grade === "TOEFL" && subject === "작문연습" && problemSet && !setMap.hasOwnProperty(problemSet)) {
        setMap[problemSet] = String(row[3] || "");
      }
    });
  }

  cache.put(cacheKey, JSON.stringify(setMap), 30);
  return setMap;
}

function findWritingSubmission_(e) {
  var studentName = String(e.parameter.studentName || "").trim();
  var problemSet = String(e.parameter.problemSet || "").trim();
  var password = String(e.parameter.password || "");

  if (!studentName || !problemSet || !password || !isWritingSetPasswordValid_(problemSet, password)) {
    return writingResultJson_({ found: false });
  }

  var sheet = getWritingSubmissionsSheet_();
  var lastRow = sheet.getLastRow();
  if (lastRow < 2) return writingResultJson_({ found: false });

  // 답안, 첨삭 프롬프트, 첨삭문처럼 긴 열은 결과를 실제로 열 때만 읽는다.
  var lookupRows = sheet.getRange(2, 2, lastRow - 1, 4).getValues();
  var submissionIds = sheet.getRange(2, 14, lastRow - 1, 1).getValues();
  for (var i = lookupRows.length - 1; i >= 0; i--) {
    if (String(lookupRows[i][0] || "").trim() !== studentName) continue;
    if (String(lookupRows[i][3] || "").trim() !== problemSet) continue;

    var submissionId = String(submissionIds[i][0] || "").trim();
    if (submissionId) {
      return writingResultJson_({
        found: true,
        submissionId: submissionId
      });
    }
  }

  return writingResultJson_({ found: false });
}

function isWritingSetPasswordValid_(problemSet, password) {
  var setMap = getWritingSetPasswordMap_();
  return setMap.hasOwnProperty(problemSet) && setMap[problemSet] === password;
}

function writingResultJson_(data) {
  return ContentService
    .createTextOutput(JSON.stringify(data))
    .setMimeType(ContentService.MimeType.JSON);
}


function doGet(e) {
  if (e.parameter.action === "getWordChain") {
    return getWordChain_(e);
  }

  if (e.parameter.action === "getCrosswordLeaderboard") {
    return getCrosswordLeaderboard_(e);
  }

  // 작문 결과 찾기 화면의 문제세트 목록
  if (e.parameter.action === "getWritingResultSets") {
    return getWritingResultSets_(e);
  }

  // 작문 제출 결과: 개인 제출번호로 원문과 첨삭만 조회
  if (e.parameter.action === "getWritingSubmission") {
    return getWritingSubmission_(e);
  }

  // 공개용 단어 결과 보기: Scores 시트에서 필요한 항목만 보내기
  if (e.parameter.action === "getPublicScoreResults") {
    return getPublicScoreResults_(e);
  }

  // 공개용 학생 결과 보기: test 시트에서 필요한 항목만 보내기
  if (e.parameter.action === "getPublicTestResults") {
    return getPublicTestResults_(e);
  }

  // 단어: 과목별로 나눈 시트만 읽기
  if (e.parameter.action === "getVoca") {
    var targetSubject = String(e.parameter.subject || "").trim();
    var ss = SpreadsheetApp.getActiveSpreadsheet();
    var vocaSheetName = getVocaSheetName_(targetSubject);
    var vocaSheet = ss.getSheetByName(vocaSheetName) || ss.getSheetByName("Voca");
    var usingFallbackVocaSheet = vocaSheet && vocaSheet.getName() === "Voca";
    var vocaResult = [];

    if (vocaSheet) {
      var vocaRows = vocaSheet.getDataRange().getValues();
      for (var i = 1; i < vocaRows.length; i++) {
        var rowSubject = String(vocaRows[i][0] || "").trim();
        var rowDay = vocaRows[i][1];
        if (rowDay && (!targetSubject || !usingFallbackVocaSheet || rowSubject === targetSubject)) {
          vocaResult.push({
            subject: String(vocaRows[i][0] || targetSubject),
            day: String(vocaRows[i][1]),
            num: Number(vocaRows[i][2]),
            question: String(vocaRows[i][3]),
            answer: String(vocaRows[i][4]),
            w1: String(vocaRows[i][5]),
            w2: String(vocaRows[i][6]),
            w3: String(vocaRows[i][7])
          });
        }
      }
    }

    return ContentService
      .createTextOutput(JSON.stringify(vocaResult))
      .setMimeType(ContentService.MimeType.JSON);
  }

  // 문제 풀이: 학년별로 나눈 Questions 시트만 읽기
  if (e.parameter.action === "getQuiz") {
    var targetSet = String(e.parameter.set || "");
    var targetGrade = normalizeGradeName_(e.parameter.grade);
    var result = { passage: "", questions: [] };
    var ss = SpreadsheetApp.getActiveSpreadsheet();
    var cache = CacheService.getScriptCache();
    var cacheKey = "quiz:" + targetGrade + ":" + targetSet;
    var cachedQuiz = cache.get(cacheKey);

    if (cachedQuiz) {
      return ContentService
        .createTextOutput(cachedQuiz)
        .setMimeType(ContentService.MimeType.JSON);
    }

    result.passage = getQuizPassage_(ss, targetSet, targetGrade);
    result.questions = getQuizQuestions_(ss, targetSet, targetGrade);

    var quizJson = JSON.stringify(result);
    if (quizJson.length < 90000) {
      cache.put(cacheKey, quizJson, 300);
    }

    return ContentService
      .createTextOutput(quizJson)
      .setMimeType(ContentService.MimeType.JSON);
  }

  // 첫 화면 목록은 Passages에서 가져오기
  var sheet = SpreadsheetApp.getActiveSpreadsheet().getSheetByName("Passages");
  var dataList = [];
  if (sheet) {
    var rows = sheet.getDataRange().getValues();
    for (var j = 1; j < rows.length; j++) {
      var row = rows[j];
      if (row[0] && row[1] && row[2]) {
        dataList.push({
          grade: normalizeGradeName_(row[0]),
          subject: String(row[1]),
          set: String(row[2]),
          password: String(row[3])
        });
      }
    }
  }

  return ContentService
    .createTextOutput(JSON.stringify(dataList))
    .setMimeType(ContentService.MimeType.JSON);
}

function getPublicScoreResults_(e) {
  var sheet = getScoresSheet_();
  var result = [];
  var limit = Math.min(Number(e.parameter.limit || 100), 200);

  if (sheet && sheet.getLastRow() > 1) {
    var rows = sheet.getDataRange().getValues();
    for (var i = rows.length - 1; i >= 1 && result.length < limit; i--) {
      var row = rows[i];
      if (!row[0] && !row[1]) continue;

      result.push({
        date: formatPublicDate_(row[0]),
        studentName: maskStudentName_(row[1]),
        subjectDay: String(row[2] || ""),
        range: String(row[3] || ""),
        score: String(row[4] || "")
      });
    }
  }

  return ContentService
    .createTextOutput(JSON.stringify({
      type: "publicScoreResults",
      rows: result
    }))
    .setMimeType(ContentService.MimeType.JSON);
}

function getPublicTestResults_(e) {
  var ss = SpreadsheetApp.getActiveSpreadsheet();
  var sheet = ss.getSheetByName("test") || ss.getSheetByName("Test");
  var result = [];
  var limit = Math.min(Number(e.parameter.limit || 100), 200);

  if (sheet && sheet.getLastRow() > 1) {
    var rows = sheet.getDataRange().getValues();
    for (var i = rows.length - 1; i >= 1 && result.length < limit; i--) {
      var row = rows[i];
      if (!row[0] && !row[1]) continue;

      result.push({
        date: formatPublicDate_(row[0]),
        studentName: maskStudentName_(row[1]),
        grade: String(row[2] || ""),
        subject: String(row[3] || ""),
        problemSet: String(row[4] || ""),
        score: String(row[8] || "")
      });
    }
  }

  return ContentService
    .createTextOutput(JSON.stringify({
      type: "publicTestResults",
      rows: result
    }))
    .setMimeType(ContentService.MimeType.JSON);
}

function formatPublicDate_(value) {
  if (!value) return "";
  var date = value instanceof Date ? value : new Date(value);
  if (isNaN(date.getTime())) return String(value);
  return Utilities.formatDate(date, Session.getScriptTimeZone(), "yyyy-MM-dd");
}

function maskStudentName_(value) {
  var name = String(value || "").trim();
  if (!name) return "";
  if (name.indexOf("즌") === 0 || name.indexOf("즈") === 0) return "Z**";
  var chars = Array.from(name);
  return getNameInitial_(chars[0]) + "**";
}

function getNameInitial_(char) {
  var first = String(char || "").trim();
  if (!first) return "";

  var surnameInitials = {
    "김": "K",
    "이": "L",
    "박": "P",
    "최": "C",
    "정": "J",
    "강": "K",
    "조": "C",
    "윤": "Y",
    "장": "J",
    "임": "L",
    "한": "H",
    "오": "O",
    "서": "S",
    "신": "S",
    "권": "K",
    "황": "H",
    "안": "A",
    "송": "S",
    "류": "R",
    "홍": "H",
    "전": "J",
    "즈": "Z",
    "즌": "Z",
    "고": "K",
    "문": "M",
    "양": "Y",
    "손": "S",
    "배": "B",
    "백": "B",
    "허": "H",
    "유": "Y",
    "남": "N",
    "심": "S",
    "노": "N",
    "하": "H",
    "곽": "K",
    "성": "S",
    "차": "C",
    "주": "J",
    "우": "W",
    "구": "K",
    "민": "M",
    "진": "J",
    "지": "J",
    "엄": "E",
    "채": "C",
    "원": "W",
    "천": "C",
    "방": "B",
    "공": "K",
    "현": "H",
    "함": "H",
    "변": "B",
    "여": "Y",
    "추": "C",
    "도": "D",
    "소": "S",
    "석": "S",
    "선": "S",
    "설": "S",
    "마": "M",
    "길": "K"
  };

  if (surnameInitials[first]) return surnameInitials[first];

  var upper = first.charAt(0).toUpperCase();
  if (/^[A-Z]$/.test(upper)) return upper;

  var code = first.charCodeAt(0);
  if (code >= 0xAC00 && code <= 0xD7A3) {
    var choseongIndex = Math.floor((code - 0xAC00) / 588);
    var initials = ["K", "K", "N", "D", "D", "R", "M", "B", "B", "S", "S", "Y", "J", "J", "C", "K", "T", "P", "H"];
    return initials[choseongIndex] || "*";
  }

  return "*";
}

function getVocaSheetName_(subject) {
  if (subject === "토플") return "Voca_토플";
  if (subject === "토익") return "Voca_토익";
  if (subject === "SAT") return "Voca_SAT";
  if (subject === "교과서_중등") return "Voca_교과서_중등";
  if (subject === "교과서_고등") return "Voca_교과서_고등";
  if (subject === "교과서") return "Voca_교과서";
  return "Voca";
}

function getQuestionSheetName_(grade) {
  grade = normalizeGradeName_(grade);
  if (grade === "중1") return "Questions_중1";
  if (grade === "중2") return "Questions_중2";
  if (grade === "중3") return "Questions_중3";
  if (grade === "고1") return "Questions_고1";
  if (grade === "고2") return "Questions_고2";
  if (grade === "고3") return "Questions_고3";
  if (grade === "TOEFL") return "Questions_TOEFL";
  if (grade === "SAT") return "Questions_SAT";
  return "Questions";
}

function normalizeGradeName_(grade) {
  var value = String(grade || "").trim();
  var upper = value.toUpperCase();
  if (value === "토플" || upper === "TOEFL") return "TOEFL";
  if (value === "사트" || upper === "SAT") return "SAT";
  return value;
}

function getQuizPassage_(ss, targetSet, targetGrade) {
  var passageSheet = ss.getSheetByName("Passages");
  if (!passageSheet || !targetSet) return "";

  var lastRow = passageSheet.getLastRow();
  if (lastRow < 2) return "";

  var setMatches = passageSheet
    .getRange(2, 3, lastRow - 1, 1)
    .createTextFinder(targetSet)
    .matchEntireCell(true)
    .findAll();

  for (var i = 0; i < setMatches.length; i++) {
    var rowNumber = setMatches[i].getRow();
    var row = passageSheet.getRange(rowNumber, 1, 1, 5).getValues()[0];
    var rowGrade = normalizeGradeName_(row[0]);

    if (targetGrade && rowGrade !== targetGrade) continue;

    var passageCell = passageSheet.getRange(rowNumber, 5);
    return resolvePassageText_(
      ss,
      row[4],
      passageCell.getRichTextValue(),
      passageCell.getBackground()
    );
  }

  return "";
}

function getQuizQuestions_(ss, targetSet, targetGrade) {
  var questionSheetName = getQuestionSheetName_(targetGrade);
  var qSheet = ss.getSheetByName(questionSheetName) || ss.getSheetByName("Questions");
  var questions = [];

  if (!qSheet || !targetSet) return questions;

  var lastRow = qSheet.getLastRow();
  if (lastRow < 2) return questions;

  var setMatches = qSheet
    .getRange(2, 1, lastRow - 1, 1)
    .createTextFinder(targetSet)
    .matchEntireCell(true)
    .findAll()
    .sort(function(a, b) {
      return a.getRow() - b.getRow();
    });

  setMatches.forEach(function(match) {
    var row = qSheet.getRange(match.getRow(), 1, 1, 9).getValues()[0];
    questions.push({
      question: String(row[1]),
      c1: String(row[2]),
      c2: String(row[3]),
      c3: String(row[4]),
      c4: String(row[5]),
      answer: String(row[6]),
      explanation: String(row[7] || ""),
      type: String(row[8] || ""),
      blankSentence: String(row[2] || ""),
      wordBank: String(row[3] || "")
    });
  });

  return questions;
}

function resolvePassageText_(ss, passageValue, passageRichTextValue, passageBackground) {
  var value = String(passageValue || "").trim();
  if (!value) return "";

  var passageTextSheet = ss.getSheetByName("PassageTexts");
  if (!passageTextSheet) return richTextToHtml_(passageRichTextValue, passageBackground) || value;

  var lastRow = passageTextSheet.getLastRow();
  if (lastRow > 1) {
    var match = passageTextSheet
      .getRange(2, 1, lastRow - 1, 1)
      .createTextFinder(value)
      .matchEntireCell(true)
      .findNext();

    if (match) {
      var textCell = passageTextSheet.getRange(match.getRow(), 2);
      return richTextToHtml_(textCell.getRichTextValue(), textCell.getBackground()) || textCell.getValue() || "";
    }
  }

  // 기존 Passages 데이터처럼 E열에 지문 본문이 직접 들어 있는 경우도 계속 지원
  return richTextToHtml_(passageRichTextValue, passageBackground) || value;
}

function richTextToHtml_(richTextValue, backgroundColor) {
  if (!richTextValue) return "";

  var runs = richTextValue.getRuns();
  if (!runs || runs.length === 0) {
    return wrapHighlight_(escapeHtml_(richTextValue.getText()), backgroundColor);
  }

  var html = "";
  runs.forEach(function(run) {
    var text = escapeHtml_(run.getText());
    var style = run.getTextStyle();
    if (style && style.isUnderline()) {
      text = "<u>" + text + "</u>";
    }

    if (isHighlightedBackground_(backgroundColor)) {
      text = "<mark>" + text + "</mark>";
    }

    html += text;
  });

  return html;
}

function wrapHighlight_(html, backgroundColor) {
  if (isHighlightedBackground_(backgroundColor)) {
    return "<mark>" + html + "</mark>";
  }
  return html;
}

function isHighlightedBackground_(backgroundColor) {
  var color = String(backgroundColor || "").toLowerCase();
  return color && color !== "#ffffff" && color !== "white" && color !== "#fff";
}

function escapeHtml_(text) {
  return String(text || "")
    .replace(/&/g, "&amp;")
    .replace(/</g, "&lt;")
    .replace(/>/g, "&gt;");
}

function sendMiddleVocaResultEmail_(e) {
  var subjectDay = String(e.parameter.subjectDay || "");

  // 단어는 중등부에서 쓰는 교과서 단어만 이메일 발송
  if (subjectDay.indexOf("중등 교과서") === -1) return;

  var title = "[대성 SKY] 중등 교과서 단어 결과 - " + (e.parameter.studentName || "");
  var body = [
    "중등부 교과서 단어 테스트 결과가 저장되었습니다.",
    "",
    "학생 이름: " + (e.parameter.studentName || ""),
    "시험 내용: " + subjectDay,
    "시험 범위: " + (e.parameter.range || ""),
    "점수: " + (e.parameter.score || ""),
    "제출 시간: " + new Date()
  ].join("\n");

  sendResultEmail_(title, body);
}

function sendMiddleTestResultEmail_(e) {
  var grade = String(e.parameter.grade || "").trim();

  // 내신/예상 문제는 중1, 중2, 중3만 이메일 발송
  if (!isMiddleGrade_(grade)) return;

  var title = "[대성 SKY] 중등 문제 풀이 결과 - " + (e.parameter.studentName || "");
  var body = [
    "중등부 내신/예상 문제 풀이 결과가 저장되었습니다.",
    "",
    "학생 이름: " + (e.parameter.studentName || ""),
    "학년: " + grade,
    "과목: " + (e.parameter.subject || ""),
    "문제 세트: " + (e.parameter.problemSet || ""),
    "전체 문제 수: " + (e.parameter.questionCount || ""),
    "맞은 개수: " + (e.parameter.correctCount || ""),
    "틀린 개수: " + (e.parameter.wrongCount || ""),
    "점수: " + (e.parameter.score || ""),
    "완료 여부: " + (e.parameter.completed || "TRUE"),
    "제출 시간: " + new Date()
  ].join("\n");

  sendResultEmail_(title, body);
}

function isMiddleGrade_(grade) {
  return grade === "중1" || grade === "중2" || grade === "중3";
}

function sendResultEmail_(title, body) {
  try {
    if (!MIDDLE_RESULT_EMAIL || MIDDLE_RESULT_EMAIL === "teacher@example.com") return;
    MailApp.sendEmail(MIDDLE_RESULT_EMAIL, title, body);
  } catch (err) {
    Logger.log("Result email failed: " + err);
  }
}

function testMiddleResultEmail() {
  if (!MIDDLE_RESULT_EMAIL || MIDDLE_RESULT_EMAIL === "teacher@example.com") {
    throw new Error("Code.gs 맨 위의 MIDDLE_RESULT_EMAIL을 실제 이메일 주소로 먼저 바꾸세요.");
  }

  MailApp.sendEmail(
    MIDDLE_RESULT_EMAIL,
    "[대성 SKY] 이메일 발송 권한 테스트",
    "이 메일이 도착하면 중등부 결과 이메일 발송 권한이 정상입니다.\n\n테스트 시간: " + new Date()
  );
}
